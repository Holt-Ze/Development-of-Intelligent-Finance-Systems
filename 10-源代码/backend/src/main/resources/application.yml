server:
  port: 8080

spring:
  application:
    name: finance-management-system
  datasource:
    url: jdbc:mysql://localhost:3306/finance_management_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  backup-dir: ./backups
